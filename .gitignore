# ── Harness runtime state (never committed) ─────────────────
# Root .board/ and root docs artifact dirs are the LIVE dev workspace when
# dogfooding the harness on itself — the shipped templates live in
# project-template/. Pristine-by-construction (ADR-0013).
/.board/
/PLAN.md
/docs/CONTEXT.md
/capiva-blueprints/
/docs/specs/
/docs/reports/
/docs/tech-context/
/docs/handover/
.state/
.claude_output/
.worktrees/

# ── Generated reports (produced by /test-verify) ─────────────
/reports/
docs/reports/*.md
coverage/
*.coverage
*.coveragexml
*.opencover.xml

# ── .NET (dotnet-hexagonal blueprint) ─────────────────────────
bin/
obj/
[Dd]ebug/
[Rr]elease/
TestResults/
*.user
*.suo
*.nupkg
*.snupkg
.vs/
.config/dotnet-tools.json

# ── Node.js (nextjs-typescript blueprint) ─────────────────────
node_modules/
.next/
out/
dist/
.turbo/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Python (python-fastapi blueprint) ─────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/
htmlcov/
.coverage.*

# ── IDEs ──────────────────────────────────────────────────────
.idea/
*.sln.iml
*.rsuser

# ── OS ────────────────────────────────────────────────────────
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
._*

# ── Secrets & local config (all stacks) ───────────────────────
*.pfx
*.key
*.pem
.env
.env.*
!.env.example
appsettings.*.json
!appsettings.json
!appsettings.Development.json
local.settings.json
/.claude/hooks/
