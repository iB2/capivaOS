# Feature Docs Index

Project-facing feature docs, one per completed task, generated by the execution
docs-generation step (`/capiva:auto`, RFN-012). Each row links to
`docs/features/<TASK-ID>.md` — "what was built + how to use it". This directory
is tracked (it ships with the project), unlike the gitignored `docs/specs/` and
`docs/reports/`.

| Task | Feature | PR |
|------|---------|----|
<!-- rows appended by the docs-generation step, e.g.: -->
<!-- | RFN-013 | [Two-sprint cycle](RFN-013.md) | #61 | -->
